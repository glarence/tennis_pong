package players;

import model.TennisPlayer;

public class peteSampras extends TennisPlayer{
	
	public peteSampras(String name, int speed, int serve, int backhand,
			int forehand, String imageURL){
		super(name, speed, serve, backhand,
			forehand, imageURL);
	}
}
